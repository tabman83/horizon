apiVersion: horizon.ninoparisi.io/v1
kind: AzureKeyVaultSubscription
metadata:
  name: ci-tooling
  namespace: ci-tooling
spec:
  k8sSecretObjectName: ci-tooling
  vaults:
  - azureKeyVaultName: esw-tooling-ci-we
    secretPrefix: cm-
